AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Application Load Balancer (HTTP) + EC2 Auto Scaling Group in private app subnets.
  ALB listens on port 80 (HTTP). For production add an ACM cert and switch to HTTPS.

Parameters:
  EnvironmentName:
    Type: String
    Default: campus-booking
  AmiId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/amzn2-ami-hvm-x86_64-gp2
    Description: Latest Amazon Linux 2 AMI
  InstanceType:
    Type: String
    Default: t3.micro
  DBMasterUsername:
    Type: String
    Default: admin
  DBMasterPassword:
    Type: String
    NoEcho: true
    Default: CampusBooking2025!
  DbEndpoint:
    Type: String
    Description: RDS endpoint (from stack 04)

Resources:

  # ── ALB ──────────────────────────────────────────────────────────────────
  ALB:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: !Sub '${EnvironmentName}-alb'
      Scheme: internet-facing
      Type: application
      Subnets:
        - !ImportValue
          Fn::Sub: '${EnvironmentName}-PublicSubnet1'
        - !ImportValue
          Fn::Sub: '${EnvironmentName}-PublicSubnet2'
      SecurityGroups:
        - !ImportValue
          Fn::Sub: '${EnvironmentName}-AlbSgId'
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-alb'

  HttpListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref ALB
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref AppTargetGroup

  AppTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub '${EnvironmentName}-tg'
      VpcId: !ImportValue
        Fn::Sub: '${EnvironmentName}-VpcId'
      Protocol: HTTP
      Port: 3000
      TargetType: instance
      HealthCheckPath: /health
      HealthCheckIntervalSeconds: 30
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3

  # ── Launch Template ────────────────────────────────────────────────────────
  AppLaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: !Sub '${EnvironmentName}-lt'
      LaunchTemplateData:
        ImageId: !Ref AmiId
        InstanceType: !Ref InstanceType
        IamInstanceProfile:
          Arn: !ImportValue
            Fn::Sub: '${EnvironmentName}-EC2InstanceProfileArn'
        SecurityGroupIds:
          - !ImportValue
            Fn::Sub: '${EnvironmentName}-AppSgId'
        MetadataOptions:
          HttpTokens: required
          HttpPutResponseHopLimit: 1
        BlockDeviceMappings:
          - DeviceName: /dev/xvda
            Ebs:
              VolumeSize: 20
              VolumeType: gp3
              Encrypted: true
        UserData:
          Fn::Base64: !Sub |
            #!/bin/bash
            set -euxo pipefail

            # Install Node.js 20
            curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
            yum install -y nodejs git

            # Clone application
            cd /opt
            git clone https://github.com/YOUR_ORG/campus-booking-aws.git campus-booking
            cd campus-booking/app/backend
            npm ci --production

            # Write environment config
            SESSION_SECRET=$(openssl rand -hex 32)
            cat > /opt/campus-booking/.env <<EOF
            NODE_ENV=production
            PORT=3000
            DB_HOST=${DbEndpoint}
            DB_NAME=CampusBookingDB
            DB_USER=${DBMasterUsername}
            DB_PASSWORD=${DBMasterPassword}
            DB_SSL=true
            AWS_REGION=${AWS::Region}
            SESSION_SECRET=$SESSION_SECRET
            EOF

            # Start with pm2
            npm install -g pm2
            pm2 start /opt/campus-booking/app/backend/app.js --name campus-booking
            pm2 startup systemd -u root --hp /root
            pm2 save

  # ── Auto Scaling Group ────────────────────────────────────────────────────
  AppASG:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      AutoScalingGroupName: !Sub '${EnvironmentName}-asg'
      LaunchTemplate:
        LaunchTemplateId: !Ref AppLaunchTemplate
        Version: !GetAtt AppLaunchTemplate.LatestVersionNumber
      VPCZoneIdentifier:
        - !ImportValue
          Fn::Sub: '${EnvironmentName}-PrivateAppSubnet1'
        - !ImportValue
          Fn::Sub: '${EnvironmentName}-PrivateAppSubnet2'
      TargetGroupARNs:
        - !Ref AppTargetGroup
      MinSize: '1'
      MaxSize: '2'
      DesiredCapacity: '1'
      HealthCheckType: ELB
      HealthCheckGracePeriod: 120
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-app'
          PropagateAtLaunch: true

Outputs:
  AlbDnsName:
    Value: !GetAtt ALB.DNSName
    Export:
      Name: !Sub '${EnvironmentName}-AlbDns'
  AlbArn:
    Value: !Ref ALB
    Export:
      Name: !Sub '${EnvironmentName}-AlbArn'
